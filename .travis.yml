language: go

go:
- 1.11.x

env:
- GO111MODULE=on

before_install:
  # gox simplifies building for multiple architectures
  - go get golang.org/x/lint/golint
  - go get github.com/mitchellh/gox

install:
  # skip

script:
  - make deps
  - make test
  - make deploy

deploy:
  provider: releases
  skip_cleanup: true
  api_key:
    secure: "${GITHUB_OAUTH_TOKEN_SECURE}"
  file:
    - bin/gateway_darwin_386
    - bin/gateway_darwin_amd64
    - bin/gateway_linux_386
    - bin/gateway_linux_amd64
    - bin/gateway_linux_arm
    - bin/gateway_linux_arm64
    - bin/gateway_windows_386.exe
    - bin/gateway_windows_amd64.exe
  on:
    repo: deps-cloud/gateway
    tags: true
